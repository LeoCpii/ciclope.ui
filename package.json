{
  "name": "lizard.ui",
  "version": "1.0.0",
  "description": "",
  "main": "dist/index.js",
  "scripts": {
    "build": "rimraf dist && npm run compile:all && npm run assets && npm run scss",

    "compile:all": "npm run compile:lib && npm run compile:api",
    "compile:lib": "tsc -p tsconfig.lib.json && tsc-alias -p tsconfig.aliases.json",
    "compile:api": "tsc -p tsconfig.api.json && tsc-alias -p tsconfig.aliases.json",

    "dev": "nodemon src/lib/index.ts",

    "scss": "npm run scss:default && npm run scss:micrablepharus && npm run scss:vanzosaura && npm run scss:custom",
    "scss:default": "node-sass src/ui/scss/themes/default.scss -o dist/themes --output-style compressed",
    "scss:micrablepharus": "node-sass src/ui/scss/themes/micrablepharus.scss -o dist/themes --output-style compressed",
    "scss:vanzosaura": "node-sass src/ui/scss/themes/vanzosaura.scss -o dist/themes --output-style compressed",
    "scss:custom": "node-sass src/ui/scss/themes/custom.scss -o dist/themes --output-style compressed",
    "clean": "rimraf dist/scss",
    "assets": "mkdirp dist/assets && cp -r src/ui/assets/* dist/assets",

    "test": "rimraf dist && tsc -p tsconfig.api.json",
    "test-clean": "rimraf dist"
  },
  "dependencies": {
    "balloon-css": "^1.2.0"
  },
  "devDependencies": {
    "@types/node": "15.0.3",
    "@types/node-sass": "^4.11.1",
    "clean-webpack-plugin": "^4.0.0-alpha.0",
    "html-webpack-plugin": "^5.3.2",
    "mkdirp": "^1.0.4",
    "node-sass": "^6.0.1",
    "nodemon": "^2.0.7",
    "storybook": "^6.4.19",
    "ts-loader": "^9.2.7",
    "ts-node": "8.10.1",
    "tsc": "^2.0.3",
    "tsc-alias": "^1.3.9",
    "typescript": "^4.6.2",
    "typings": "2.1.1"
  },
  "author": "Leonardo Gonçalves",
  "license": "ISC"
}
